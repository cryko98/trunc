# 🌴 TRUNC — The Troll Uncle Memecoin

Editorial poster-style landing page for **$TRUNC**, a Solana memecoin.

Pure HTML + CSS + vanilla JS. No frameworks, no build step. Open `index.html` and it runs.

## Files
- `index.html` — markup
- `style.css` — all styles
- `script.js` — copy-to-clipboard, mobile nav, drag-scroll memes, konami easter egg
- `logo.jpg`, `banner1.png`, `trun.png`, `trunc*.jpg` — assets

## Before launch (TODO)
1. Replace the placeholder contract address (`xxxxxxxxxxxxxxxxxxxxxxxxxxxxx`) in:
   - The hero CA box (`#caAddr`)
   - The footer CA box (`#caAddrFooter`)
   - The DexScreener iframe `src` in the chart section
2. Replace the X.com placeholder URL (`https://x.com/`) with the official handle.
3. Replace the pump.fun placeholder URL (`https://pump.fun/`) with the token's pump.fun page.
4. Consider converting `.jpg` images to `.webp` for better load performance.

## Deploy
Static — works on GitHub Pages, Netlify, Vercel, or any static host. For GitHub Pages, push to `main` and enable Pages in repo Settings → Pages → main / root.

## Easter egg
↑↑↓↓←→←→BA. You're welcome.
